PORT=5001
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/sarvhit
JWT_SECRET=your_jwt_secret_key_change_in_production
JWT_EXPIRE=7d

# CORS Configuration
# Primary frontend URL for development (localhost:5173)
CORS_ORIGIN=http://localhost:5173

# For production deployments, set this to your frontend domain
# FRONTEND_URL=https://yourdomain.com

# Development: Vite uses multiple ports if previous ones are in use (5174, 5175, etc.)
# The server automatically allows these ports
